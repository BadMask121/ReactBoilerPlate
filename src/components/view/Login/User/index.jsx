import React from "react";
import PropTypes from "prop-types";
import Header from "components/custom/Header";
const User = props => {
  return (
    <div>
      <div>User</div>
    </div>
  );
};

User.propTypes = {};

export default User;
